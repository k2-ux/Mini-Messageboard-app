<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
        body { font-family: Arial, sans-serif; max-width: 600px; margin: 0 auto; padding: 20px; }
        .message { border: 1px solid #ddd; padding: 20px; border-radius: 5px; }
        .btn { display: inline-block; padding: 8px 15px; background: #007bff; color: white; text-decoration: none; border-radius: 4px; }
    </style>
</head>
<body>
    <h1><%= title %></h1>
    
    <div class="message">
        <h2>Message from <%= message.user %></h2>
        <p><%= message.text %></p>
        <small>Posted: <%= message.added.toLocaleString() %></small>
    </div>
    
    <br>
    <a href="/" class="btn">Back to Messages</a>
</body>
</html>